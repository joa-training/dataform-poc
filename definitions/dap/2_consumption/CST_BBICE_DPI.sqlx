config {
  type: "incremental",
  schema: "cst_banking",
  name: "BBICE_DPI",
  tags: ["BBICE_DPI_cst", "cst"],
  bigquery: {
    partitionBy: "FECHA_PROCESO"
  }
}

SELECT *
FROM `${dataform.projectConfig.defaultDatabase}.std_banking.OPF_TD_MAS`
WHERE CAST(FECHA_PROCESO AS DATE) = DATE('${dataform.projectConfig.vars.process_date}')
